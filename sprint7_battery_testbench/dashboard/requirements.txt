# Streamlit Community Cloud installs from this file (it sits next to app.py).
# The C++ backend is optional and NOT required to run the dashboard.
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pandas>=2.0
streamlit>=1.30
